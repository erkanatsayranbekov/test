name: tests

on: [push, pull_request]

jobs:
  build:
    name: tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Prepare dependencies
        run: |
          go mod download
      - name: Test code
        run: |
          go test ./...
